export default function TasksPage() {
  return (
    <section className="panel">
      <h3>Task Execution UI</h3>
      <p>Task history and live statuses are available from the backend endpoint <code>/api/v1/agents/tasks/:id</code>.</p>
    </section>
  );
}
